{
  "name": "winfm-file-manager",
  "version": "1.0.0",
  "private": true,
  "main": "file-manager.js",
  "scripts": {
    "start": "node file-manager.js",
    "build:assets": "esbuild scripts/vendor-entry.js --bundle --minify --format=esm --outfile=src/static/material-web.js && sh scripts/fetch-font.sh"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "devDependencies": {
    "@material/web": "^2.4.1",
    "esbuild": "^0.28.0"
  }
}
