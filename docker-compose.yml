services:
  file-manager:
    build: .
    container_name: file-manager
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ${DATA_DIR}:/data
    environment:
      # 可选：Basic Auth 访问认证，格式 user:pass
      - FM_AUTH=${FM_AUTH:-}
